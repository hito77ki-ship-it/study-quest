# 本番表示・内部リンク監査

実施日: 2026年8月11日

## 1. 本番表示確認

前回の画像未配備を修正し、カテゴリカード用の18枚（JPEG/WebP）がリポジトリに追加されていることをローカルで確認した。`article-widgets.js` が参照するサムネイルは、HTML記事と同じ作業ツリー上で全件存在する。

読み取り専用の外部ページ確認では、公開トップページと `boki1.html` が取得でき、サイトタイトル・本文の応答を確認した。ただし、取得結果は検索側のキャッシュを含む可能性があり、今回の作業ツリーの変更がGitHub Pagesへ反映されたことまでは確認できない。そのため、画像の切れ方・キャッシュ反映・モバイル表示は、公開後のブラウザ確認を残す。

## 2. 内部リンク監査

### 機械検査

- HTMLファイル数: 213
- HTML・画像のローカル参照切れ: 0件
- `article-widgets.js` の記事キー: 225件（重複キーは独自性メモの後段定義による上書き）
- 記事キーから実ファイルが見つからないもの: 0件

### 孤立候補の分類

| ページ | 判定 |
|---|---|
| `admin.html` | 管理画面。`noindex,nofollow` で意図的に検索対象外 |
| `boki3-ginko.html` | `boki2-ginko-chosei.html` への移転ページ。`noindex` で意図的に検索対象外 |
| `flyer.html` | 印刷・配布用アセット。`noindex` で意図的に検索対象外 |
| `game.html` | RPG本体の独立ページ。`noindex` で意図的に検索対象外 |
| `google9c49256384d9c83a.html` | Search Console所有権確認ファイル。記事ではない |

以上の5件は、記事の孤立ではなく、管理・移転・配布・アプリ・所有権確認のためのシステムページだった。現時点で内部リンク修正の稟議は起こさない。

## 3. 残る確認

1. GitHub Pages反映後にトップページをハードリロードし、カテゴリカードの画像が表示されることを確認する。
2. モバイル幅で代表記事カードの画像比率、タイトルの折返し、代替テキスト露出がないことを確認する。
3. Search ConsoleのURL検査でトップページと代表ハブ記事の再クロールを依頼する。

## CEO所見

今回の内部リンク監査で、404と記事孤立は検出されなかった。優先すべき問題はリンク網ではなく、外部公開後の画像反映確認と、表示回数があるのに選ばれていない記事の本文価値である。次段では、GSCで表示が続く記事へ、ページ固有の判断手順を直接追加する。

ステータス: **ローカル監査完了・本番目視確認待ち**
