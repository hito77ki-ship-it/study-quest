# サムネイル台帳と画像フォールバックの整合：実施報告

**実施日：** 2026年9月6日
**対象：** 記事カード、トップ一覧、トップ検索
**対応稟議：** `company/decisions/20260906-サムネイル台帳と画像フォールバックの整合.md`

## 実施内容

- `boki-benkyo-basho.html`、`boki-net-test-mac-ipad.html`、`boki-note-sekkei.html`、`boki2-3kyu-fukushu.html`、`boki2-shogyo-enshu.html` の5件について、カードが最初から専用JPEGを参照するようにした。SVGの404を待ってから切り替える経路をなくした。
- `article-widgets.js` の記事内カードでは、明示した専用JPEG、記事図解、従来サムネイルの順で参照するようにした。すでに表示中の同一画像をフォールバックとして再試行しない。
- `index.html` のトップ一覧・検索は同じ5件の専用JPEGを初期参照するように統一した。
- `images/article-diagrams/manifest.json` に5件を `render: "photo"` として登録し、台帳と実画像の対応を検証できるようにした。
- 商業簿記の演習記事には、読者向けの文字・ロゴを含まない640×360の学習机サムネイルを `images/article-thumbnails/boki2-shogyo-enshu.jpg` として追加した。Dark Mode Digital / Field Systemに合わせ、深色面と控えめなライムグリーンのグリッドを用いた。
- 資格一覧ヒーローは既存CSSのField Systemグリッドが正規実装であり、画像ファイルを追加しない。監査もこの実装を認識するようにした。

記事本文・広告設定・外部サービス・構造化データ・更新日・サイトマップは変更していない。

## 検証

| 検証項目 | 結果 |
|---|---|
| サムネイル台帳監査 | 合格。`articles=206`、`manifest=206`、欠落・不正SVG・不足画像・台帳不一致・実装不一致はすべて0件。 |
| 構文・形式 | 合格。`article-widgets.js` と監査スクリプトの構文、`manifest.json` のJSONを確認。 |
| 記事品質・広告ポリシー | 合格。対象5記事の品質・表現監査は指摘0件、サイト全体のAdSenseポリシー監査もブロッカー0件。 |
| PC表示 | 合格。1280px幅でトップ一覧・検索に5件のJPEG初期参照が反映され、商業簿記の検索結果は640×360で読み込まれた。ブラウザの読み込みエラーは0件。 |
| モバイル表示条件 | 合格。既存の`max-width:640px`でカードは2列、画像は`width:100%; height:auto`。`max-width:480px`の検索サムネイルは80×45pxで、今回の画像に固定幅・横スクロール要因はない。 |
| 差分 | 合格。`git diff --check`を実行。 |
