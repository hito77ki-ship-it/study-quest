# 本番・モバイル表示・公開反映ゲート監視（2026年8月15日）

**監視者：** CEO ジョブズ  
**実行範囲：** 読み取り検査、実画面確認、公開反映状態の記録のみ。記事・アプリ・本番・GitHubへの変更、commit、push、mergeは行っていない。  
**日付の注記：** 運用規約に従い `git log` の直近コミット時刻（2026-08-14 05:33 JST）も確認した。監査スクリプトが実行時刻基準でこの `20260815` ファイルを生成したため、ファイル名は変更していない。

## 0. 結論

| 区分 | 正常 | 異常 | 未確認 |
|---|---:|---:|---:|
| 本番HTTP・メタデータ（9ページ） | 9 | 0 | 0 |
| 主要画像（代表記事・検索結果・トップ） | 3導線 | 0 | 0 |
| モバイル実画面（390×844） | 8ページ | **1ページ** | 0 |
| 公開反映ゲート（図解サムネイル／要点図解） | 0 | 0 | 2対象 |

**最優先事項：** トップページ（`/`）の390px幅で、表示幅390pxに対して文書幅516pxとなる横スクロールを再現した。前回起案済みの[本番トップのモバイル横スクロール是正稟議](../../decisions/20260814-本番トップ_モバイル横スクロール是正.md)は、引き続き取締役の承認待ちである。

図解サムネイル統一（`86befab`）と記事タイトル直下の要点図解（`7a88f13`）は、いずれも `origin/main` に未到達で、当該ブランチのオープンPRもない。公開反映は**PR/main待ち**であり、本番の旧JPG／カテゴリ画像はデプロイ・キャッシュ事故とは判定しない。

## 1. 静的検査

`node scripts/audits/run-media-audits.mjs --mode production` を実行した。

- スクリプト内のNode fetchは外部DNS解決に失敗し、対象8ページを `fetch failed` と出力した。
- 同じ対象を外部HTTPで再検査すると、トップ・代表6記事・検索・アプリの9ページと主要カテゴリJPEGの全10 URLが **HTTP 200** だった。従って、スクリプトの失敗は監視実行環境のDNS制約であり、本番HTTP障害ではない。
- HTTP 404、画像404、JSON-LD構文エラーは確認していない。未展開JavaScriptテンプレート文字列は本番異常として数えていない。

## 2. 本番メタデータ・画像確認（実ブラウザ）

390px幅の実ブラウザでDOMと表示領域を確認した。全9ページでtitle・canonicalは存在し、JSON-LDの構文エラーは0件だった。検索ページは仕様どおり `og:image` とJSON-LDを持たず、アプリページはJSON-LDを持たない。ほかの7ページは `og:image` を持つ。

| ページ | HTTP | title / canonical | og:image | JSON-LD | 表示領域の画像失敗 |
|---|---:|---|---|---:|---:|
| `/` | 200 | あり / あり | あり | 1 / エラー0 | 0 |
| `boki.html` | 200 | あり / あり | あり | 3 / エラー0 | 0 |
| `boki1.html` | 200 | あり / あり | あり | 3 / エラー0 | 0 |
| `boki2.html` | 200 | あり / あり | あり | 3 / エラー0 | 0 |
| `boki3-progress.html` | 200 | あり / あり | あり | 3 / エラー0 | 0 |
| `fp3.html` | 200 | あり / あり | あり | 3 / エラー0 | 0 |
| `shihoshoshi.html` | 200 | あり / あり | あり | 3 / エラー0 | 0 |
| `search.html?q=IELTS` | 200 | あり / あり | 未設定（検索ページ） | 0 / エラー0 | 0 |
| `app.html` | 200 | あり / あり | あり | 0 / エラー0 | 0 |

検索結果のサムネイルは、遅延読込の完了を待って `112×63px`（16:9）で3件とも全体表示・読込完了を確認した。最初のDOM採取時に未完了だった `shikaku-list.jpg` も1.5秒後には正常に読込済みであり、画像失敗ではない。

## 3. モバイル実画面検証（390×844）

| 導線・画面 | 結果 | 根拠 |
|---|---|---|
| トップ | **異常（継続）** | `scrollWidth=516px`、表示幅390px。`.phones` の右端405pxと記事カード内リンクの右端516pxにより、横スクロールを再現。 |
| 代表・最近更新記事6本 | 正常 | `boki.html`、`boki1.html`、`boki2.html`、`boki3-progress.html`、`fp3.html`、`shihoshoshi.html` はすべて `scrollWidth=390px`。タイトル、本文、カテゴリビジュアルを読め、表示領域の画像失敗はない。 |
| `boki.html` の記事入口 | 正常 | タイトル直下の現行カテゴリ画像は356×200px（16:9）で全文幅を越えず、本文開始を妨げない。関連記事要素21件と「無料で始める」導線を確認。 |
| 検索 `IELTS` | 正常 | 検索結果1件を表示。結果カードのサムネイルは16:9で切り抜きなく、タイトル・要約・更新日を読める。横方向にはチップの内部スクロールがあるが、文書全体幅は390pxで横スクロールなし。 |
| 記事→Study Quest CTA | 正常 | `boki.html` の「無料で始める」を実際に選択し、`https://study-quest.net/app.html` への遷移を確認。 |
| アプリ `app.html` | 正常 | 文書幅390pxで操作対象は画面内。オフキャンバスのナビゲーションドロワーは非表示状態でも文書幅を広げていない。 |

### 図解の本番状態

- `boki.html` のタイトル下は現行のカテゴリWebPであり、計画済みの要点SVGではない。
- 検索結果は現行の `images/article-thumbnails/*.jpg` を表示している。16:9の全体表示・文字の可読性・取得は正常だが、図解SVGの本番確認はできない。
- これは次節のとおり対象commitがmain未到達のためである。

## 4. 公開反映ゲート

| 対象 | ローカル | 作業ブランチ→origin | PR→main | main→本番 | 判定 |
|---|---|---|---|---|---|
| 図解サムネイル統一（`86befab`） | commit済み | `origin/codex/adsense-cta-batch` に到達 | オープンPRなし、`origin/main` 未到達 | 判定対象外 | **PR/main待ち** |
| 記事タイトル直下要点図解（`7a88f13`） | commit済み | `origin/codex/adsense-cta-batch` に到達 | オープンPRなし、`origin/main` 未到達 | 判定対象外 | **PR/main待ち** |
| 現在ブランチの監視commit（`4f91db1`） | commit済み | originに到達 | `origin/main` 未到達 | 判定対象外 | **PR/main待ち** |
| 作業ツリーの未commit変更 | 開始時13件、終了時22件 | 対象外 | 対象外 | 対象外 | **ローカル未commit** |

### Git / GitHub根拠

- 現在ブランチは `codex/adsense-cta-batch`、upstreamは `origin/codex/adsense-cta-batch`、ahead/behindは0/0。
- 安全な `git fetch` 後も、`origin/main` は `db030e3`、現在HEADは `4f91db1`。`86befab`、`7a88f13`、`4f91db1` のいずれも `origin/main` の祖先ではない。
- GitHubで同ブランチをheadに持つオープンPRを確認した結果は0件。過去のPR #5は `aa10db5` を `db030e3` へマージ済みであり、現在の未到達commitを含まない。
- `company/decisions/20260813-記事カード_写真調サムネイルの是正と図解カード統一.md` の「本番デプロイ待ち」という状態表記は、上記の現行Git根拠とは一致しない。今回の公開反映判定は**PR/main待ち**を正とする。

## 5. 稟議

- 継続：[本番トップのモバイル横スクロール是正](../../decisions/20260814-本番トップ_モバイル横スクロール是正.md) — 取締役の承認待ち。今回も同じ1件を再現した。
- 新規稟議：なし。HTTP、JSON-LD、画像取得失敗、main到達後の本番未反映は検出していないため、重複起案しない。

## 次の確認

1. トップ横スクロール是正案の承認後、390px幅で `scrollWidth === clientWidth` とヒーロー・記事カード・CTAの欠けなしを再確認する。
2. 図解サムネイル／要点図解のオープンPR作成とmain反映後、トップ・検索・代表記事を再検証する。その時点で旧画像が残る場合のみ「main到達・本番待ち」としてデプロイ／キャッシュを調査する。
