# 本番・モバイル表示・公開反映ゲート監視（2026年8月14日）

**監視者：** CEO ジョブズ  
**実行範囲：** 読み取り検査、実画面確認、公開反映状態の記録のみ。記事・アプリ・本番・GitHubへの変更、commit、push、mergeは行っていない。

## 0. 結論

| 区分 | 正常 | 異常 | 未確認 |
|---|---:|---:|---:|
| 本番ページ／メタデータ（代表8ページ＋検索） | 9 | 0 | 0 |
| 表示画像（表示領域） | 9 | 0 | 0 |
| モバイル実画面（390px幅） | 4 | **1** | 0 |
| 公開反映ゲート（図解サムネイル・記事ヒーロー） | 0 | 0 | 1 |

**最優先事項：** トップページ（`/`）の390px幅で、表示幅390pxに対して文書幅516pxとなる横スクロールを再現した。読者が意図せず横にずれる実害があるため、[本番トップのモバイル横スクロール是正稟議](../../decisions/20260814-本番トップ_モバイル横スクロール是正.md)を起案した。

図解サムネイルと記事タイトル直下の要点図解は、現在の `codex/adsense-cta-batch` にはcommit済み・originへpush済みだが、`origin/main` に未到達であり、**公開反映は「PR/main待ち」**である。本番の旧JPG表示はデプロイ／キャッシュ事故ではない。

## 1. 静的検査

`node scripts/audits/run-media-audits.mjs --mode production` を実行した。

- 実行環境の外部DNS失敗により、対象8ページはすべて `fetch failed` となった。
- 同一時刻にブラウザ実画面では9ページが正常に開けたため、この結果は**監視実行環境の通信失敗**として扱う。本番HTTP障害とは判定しない。
- ブラウザで後述のメタデータ・JSON-LD・表示画像を再確認した。未展開JavaScriptテンプレート文字列は検出なし。

## 2. 本番メタデータ・画像確認（実ブラウザ）

Chromeで `https://study-quest.net/` を実際に読み込み、タイトル・canonical・OGP・JSON-LDをDOMから確認した。各ページは正常に表示され、表示領域内の画像の読込失敗は0件だった。

| ページ | title | canonical | og:image | JSON-LD | 表示画像失敗 |
|---|---|---|---|---:|---:|
| `/` | あり | あり | あり | 1件／構文エラー0 | 0 |
| `boki.html` | あり | あり | あり | 3件／構文エラー0 | 0 |
| `boki1.html` | あり | あり | あり | 3件／構文エラー0 | 0 |
| `boki2.html` | あり | あり | あり | 3件／構文エラー0 | 0 |
| `boki3-progress.html` | あり | あり | あり | 3件／構文エラー0 | 0 |
| `fp3.html` | あり | あり | あり | 3件／構文エラー0 | 0 |
| `shihoshoshi.html` | あり | あり | あり | 3件／構文エラー0 | 0 |
| `app.html` | あり | あり | あり | 0件／構文エラー0 | 0 |
| `search.html?q=IELTS` | あり | あり | 未設定（検索ページ） | 0件／構文エラー0 | 0 |

## 3. モバイル実画面検証（390×844）

| 導線・画面 | 結果 | 根拠 |
|---|---|---|
| トップ | **異常** | `scrollWidth=516px`、表示幅390px。`.phones` が右端405px、記事カード要素が右端516pxまで到達し、横スクロールを再現。 |
| 代表記事 `boki.html` | 正常 | 横スクロールなし。タイトル、本文、カテゴリービジュアルが読め、関連記事リンク2件とStudy Quest CTAを確認。 |
| 検索 `IELTS` | 正常 | 検索結果4件を表示。サムネイルは112×63px（640×360の16:9比率）で切り抜きなし、タイトル・要約・更新日を画面上で読める。 |
| 記事→Study Quest CTA | 正常 | `boki.html` の「無料で始める」を実際に選択し、`app.html` への遷移を確認。 |
| アプリ `app.html` | 正常 | 横スクロールなし。操作対象が画面内に収まり、ダッシュボードを表示。 |

### 図解の本番状態

- `boki.html` のタイトル下は既存カテゴリ画像（WebP）で、計画済みの要点SVGではない。
- `search.html?q=IELTS` は `images/article-thumbnails/*.jpg` を表示している。16:9の全体表示・テキスト可読性は正常だが、図解SVGの本番確認はできない。
- これは次節のとおり、対象commitがmain未到達のためである。

## 4. 公開反映ゲート

| 対象 | ローカル | 作業ブランチ→origin | PR→main | main→本番 | 判定 |
|---|---|---|---|---|---|
| 図解サムネイル統一（`86befab`） | commit済み | `origin/codex/adsense-cta-batch` に到達 | PRなし | 判定対象外 | **PR/main待ち** |
| 記事タイトル直下要点図解（`7a88f13`） | commit済み | `origin/codex/adsense-cta-batch` に到達 | PRなし | 判定対象外 | **PR/main待ち** |
| main最新 | — | `db030e3`（PR #5、CTA固有化） | main到達済み | 本番の現行表示を確認 | **本番確認済み** |

### Git根拠

- 現在ブランチ：`codex/adsense-cta-batch`。upstreamは `origin/codex/adsense-cta-batch` でahead/behindは0。
- `86befab` と `7a88f13` はともに `origin/main` の祖先ではない。
- GitHub上のPR一覧では、当該ブランチをheadにしたPRは存在しない。直近のmain反映はPR #5（`aa10db5`）のみ。
- ローカルには他作業者の未commit変更が11件あった。この監視の根拠には含めず、**ローカル未commit**として分離した。

## 5. 稟議

- 新規：[本番トップのモバイル横スクロール是正](../../decisions/20260814-本番トップ_モバイル横スクロール是正.md)
- 既存：[記事カードの写真調サムネイルを是正し、図解カードへ統一](../../decisions/20260813-記事カード_写真調サムネイルの是正と図解カード統一.md) — 実装完了／PR-main待ちであることを再確認。

## 次の確認

1. トップの横スクロール是正案を取締役が承認後、PC・390px幅で再目視する。
2. 図解カード・記事ヒーローのPRを作成しmainへ到達させた後、トップ・検索・代表記事を再検証する。その時点で旧JPGが残る場合のみ「main到達・本番待ち」としてデプロイ／キャッシュを確認する。
