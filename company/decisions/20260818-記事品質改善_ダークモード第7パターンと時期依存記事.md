# 稟議書：ダークモード破綻の第7パターン（3記事）と、「今日は7月」と書いている記事

**起案日：** 2026年8月18日
**起案者：** CEO ジョブズ
**ステータス：** 🕐 **取締役の承認待ち**
**出典：** `company/reports/20260818-記事品質監査.md`（1巡目14回目・1巡完了）

---

## ⚠️ **本稟議の指摘は 2 件である。**

| # | 対象 | 内容 | 重み |
|---|---|---|:-:|
| 1 | `univ-boki-units.html` / `boki-zero-05.html` / `boki-zero-03.html` | **ダークモードのコントラスト破綻3件**（うち1つは新パターン） | 🔴 |
| 2 | `boki1-11month-strategy.html` | 記事本文と図解2枚が「**今日は7月**」と書いている | 🔴 |

**2件目まで読んでから判断していただきたい。**

## ⚠️ 件数の範囲について

**以下は今回監査した15本の中での検出数である。** ただし指摘1の3件はいずれもサイト横断 `grep` で全体件数を確定させてあり、各項に記載した。指摘2も全記事スキャン済みである。

---

## 指摘1｜🔴 ダークモードのコントラスト破綻3件

ライト・ダーク両テーマで、最終的に適用される前景色と背景色を解決してコントラスト比を計算した。`linear-gradient()` / `radial-gradient()` の解決、`!important` の優先、`var()` のテーマ別変数表引き（フォールバックは変数定義が無いときのみ使用）を実装している。

### 1-1. 🔴 **第7パターン** — `.merit-card p` で **1.71:1**（`univ-boki-units.html`）

```
widgets 1484: html[data-theme="dark"] .merit-card{background:linear-gradient(180deg,#151A2A,#111728) !important;}
widgets 1485: html[data-theme="dark"] .merit-card .title{color:#F8FAFC !important;}
widgets 1486: html[data-theme="dark"] .merit-card .desc {color:#9BA7BB !important;}
                                                   ↑ 記事は .title / .desc を使っていない

記事 120行: .merit-card h3{color:var(--text)}   → 変数が追従して無傷 ✅
記事 121行: .merit-card p {color:#374151}       → 16進で固定 → 1.71:1 ❌
```

**08-14に検出した `.app-cta h3`（widgets が `p` と `h2` を列挙、記事は `h3` を使う）と構造が完全に同一である。** 08-14はライト側、今回はダーク側。

**そして今回、反例が同じ監査範囲の中にあった。** `.merit-card` を使う記事はサイト全体で2本しかない。

| ファイル | 使っている子要素 | 結果 |
|---|---|---|
| `boki-zero-02.html` | `.icon` / `.title` / `.desc` | ✅ **無傷**（widgetsの列挙と一致） |
| `univ-boki-units.html` | `h3` / `p` | ❌ **1.71:1** |

**同じクラス名・同じ16進直書き・おそらく同じテンプレート由来の2本で結果が分かれた。差は「widgets が列挙した子要素名と一致しているか」だけである。**

潰れているのは h2「5つの神メリットを深掘り解説」の本文——記事の主要セクションである。

### 1-2. 🔴 第3パターンの再来 — `.grad-card` で **h3 1.37 / p 1.35**（`boki-zero-05.html`）

```
記事 118行: .grad-card{background:linear-gradient(135deg,#F0FFF4,#E8F7B8)}  ← 明るい緑を固定
記事 121行: .grad-card p{color:#374151}
widgets 1520: html[data-theme="dark"] .grad-card p{color:#CCD6E5 !important;}
widgets:      html[data-theme="dark"] .grad-card{background:...}  ← 存在しない
```

**widgets が `.grad-card` について持っている規則は 1520行の1行だけである**（`grep -n 'grad-card' article-widgets.js` の結果が1行）。文字色だけ明るくして背景を暗くしないため、**明るい緑地に明るいグレー文字**になる。

08-11に検出した `.formula` とまったく同じ形。該当はサイト全体で **`boki-zero-05.html` 1本のみ**。

中身は h3「**次のステップ：日商簿記3級から始めよう**」——ゼロから簿記シリーズ全5回を読み切った読者が最後に見る**出口導線のカード**である。

### 1-3. 🔴 `.faq-item h3` で **1.47:1**（`boki-zero-03.html`）

```
記事 124行: .faq-item{background:#fff}                                       ← 白を固定
widgets 2378: html[data-theme="dark"] .faq-item{border-color:... !important}    ← 枠線だけ
widgets 2379: html[data-theme="dark"] .faq-item h3{background:rgba(255,255,255,.04) !important;
                                                    color:#CCD6E5 !important;}
```

ダークで箱が白のまま残り、**質問見出しだけが明るいグレーになる。** `.faq-item p{color:#374151}` は白の上で読めるため、**FAQの答えは読めて問いが読めない。**

該当はサイト全体で **2本**（`boki-zero-03.html` / `boki3-shohi.html`）。

### 1-4. 🕐 `.app-cta h3` ライト **1.89:1** — 08-14稟議が未実行のまま今回15本全部で再検出

`article-widgets.js` に `.app-cta h3` の記述は **0件**。904〜910行は `.app-cta p` と `.app-cta h2` だけを再着色しており、08-14に報告した194ファイルの状態がそのまま残っている。稟議 `20260814-記事品質改善_appctaライトモード破綻194本.md` は**承認待ち（4日経過）**。

### 1-5. 🟡 ブランド緑ボタンの白文字 **2.05:1**（**破綻ではない・取締役判断**）

`.nav-cta` `.faq-q` `.vs-circle` `.merit-num` `.star-badge` `.route-num` `.reason-num` はいずれも `background:var(--green)`（#8CC63F）に `color:#fff` で、**両テーマ共通で 2.05:1**。

**これはテーマの食い違いによる破綻ではなく、意図的なブランド配色である。** 1-1〜1-4 とは性質が違う。WCAG AA（通常テキスト4.5:1／大きい文字3:1）は下回るが、直せばサイト全体の見た目が変わる。

**⚠️ 私はこれについて改善案を書かない。** 実際の見え方を確認できていないうえ、ブランドの中核配色を変えるかどうかはCEOの裁量を超える。**事実だけ報告する。**

### 1-6. ⚠️ 偽陽性を2件棄却した。検査要件に1行足す必要がある

初回スクリプトは `.app-cta a`（2.03:1）と `.study-guide-cta a`（1.25:1）を出したが、**どちらも偽陽性**だった。

```
記事: .study-guide-cta{background:linear-gradient(135deg,#0A1A08,#1A3520)}   ← 親（暗い）
記事: .study-guide-cta a{background:var(--green); color:#0A0A0F}            ← a 自身が背景を持つ
```

スクリプトが `a` の文字色を**親の背景**に合成していた。実際は `a` 自身の緑背景に載るため約10:1で無傷。

**検査要件に追加：「テキストを持つ要素が自分自身の `background` を持つ場合、親ではなくそれに合成する」。**

08-14に「`.sq-chat-bubble` 等が3週連続で同じ偽陽性を出している。毎回手で棄却するのをやめる」と書いた。**今回は別種の偽陽性が新規に出た。原因は同じで、合成先の解決が不完全であること。CTAボタンは全記事にあるため、この1行を入れないと毎週200件近い偽陽性が出る。**

### 破綻パターンの一覧（7種類）

| # | 発見 | 記事側 | widgets側 | 壊れる側 |
|---|---|---|---|---|
| 1 | 08-04 | — | カンマ区切りセレクタのテーマ限定漏れ | ライト |
| 2 | 08-05/06/07 | 16進背景＋dark未対応 | 独自クラス名を知らない | ダーク |
| 3 | 08-11(9) | 16進背景＋16進文字 | `strong` の色だけ持つ | ダーク |
| 4 | 08-11(10) | 16進背景＋`var()`文字 | 変数をダークで再定義 | ダーク |
| 5 | 08-13 | `rgba()` 半透明＋`var()`文字 | `.article-hero` 規則が0件 | ダーク |
| 6 | 08-14 | `var()`文字（h3） | 背景を差し替え、`p` と `h2` だけ再着色 | ライト |
| **7** | **08-18** | **16進文字（p）** | **背景を差し替え、`.title` と `.desc` だけ再着色** | **ダーク** |

**第6と第7は同一の欠陥である。「widgets が救済のために列挙した子要素名と、記事が実際に使っている子要素が食い違う」。** 6はライト側、7はダーク側というだけの違いで、原因は1つ。

**⚠️ この発見は、検査の方針に影響する。** これまで7回、パターンを1つ見つけるたびに検査条件を継ぎ足してきた。しかし**第6と第7が同じ欠陥だったということは、パターンを数え上げる方針そのものが間違っていた**ことを意味する。08-11に出した結論——「検査条件はクラス名でも出自でもなく、最終的に適用される前景色と背景色のペアを両テーマで解決してコントラストを計算する以外にない」——は正しく、**それを実装しさえすれば7種類を1つの条件で捕まえられる。** 今回のスクリプトはそれをやったから、パターンの区別なく3件を出せた。

---

## 指摘2｜🔴 `boki1-11month-strategy.html` が「今日は7月」と書いている

### 検出内容

| 箇所 | 記述 |
|---|---|
| 本文 211行 | 下の図解が、**今日（7月）**から来年6月の本番試験までの全体戦略です |
| 本文 200行 | 残り時間　**約4ヶ月**　**7月〜11月**。一発合格はキツいが「中間ボス戦」として使うには最高のタイミング |
| 本文 200行 | 合計期間　**約11ヶ月**　**7月〜来年6月**。合計約450時間で1級を完全攻略する現実的な長期計画 |
| h2 | STEP 1——今年11月までの**4ヶ月プラン（7月〜11月）** |
| `images/boki1-11month-strategy.svg` | 「**7月〜来年6月**・合計約450時間のロードマップ」「STEP1 ▶ 今年11月（慣らし受験）**約4ヶ月**」「**7月スタート**　いざ1級へ！」「**7月〜9月**　基礎インプット」 |
| `images/boki1-11month-strategy-mobile.svg` | 同上 |

**本日は 2026年8月18日。** `boki1-next-exam.html` によれば2026年11月の簿記1級統一試験は **11月15日** で、残りは**約3か月**である。

**記事は1か月多い前提で月別プランを組んでおり、図解2枚にも「7月」が焼き込まれている。**

### なぜ気づかれなかったか

**この記事の `dateModified` は 2026-08-13 で、5日前に更新の手が入っている。**

日付を3箇所セット（JSON-LD・本文表示・sitemap）で動かすルールは2026-08-11に確立し、機能している。実際この記事の本文表示も「更新日：2026年8月13日」に更新されていた。

**しかし、本文中の相対的な時期表現はそのルールの対象外である。** 「今日（7月）」「残り約4ヶ月」「7月スタート」は、日付フィールドではなく本文の一部であり、日付更新スクリプトは触らない。

**日付の形式（3箇所セット）は守られ、日付の意味（記事が前提にしている「今」）だけが1か月ずれていた。** 2026-08-12の事故は「形式は守れていたのに値が誤っていた」だったが、今回は逆で「値は正しいのに、その値と本文の前提が矛盾している」。

### 実害の向き

title / h1 は「**今年の11月に簿記1級は間に合う？**」という時期依存の問いを立てている。**この記事は読まれる時期によって答えが変わる設計**であり、8月に読む人には「残り3か月」が答えになる。

記事は「残り約4ヶ月」を根拠に「一発合格はキツい→11月は慣らし受験にせよ」と結論している。**残りが3か月なら結論の方向は同じだが、STEP1の月別配分（7月〜9月 基礎インプット／10月〜11月 実戦）はそのまま使えない。** 8月に始める読者は、記事どおりに進めると10月末に基礎が終わり、実戦が2週間しか取れない。

08-11の `itp.html`（title「ITパスポートは**2ヶ月で受かる**？」）と同型のリスクである。

### 全記事スキャン結果

「今日（○月）」「残り時間 約○ヶ月」「今から○ヶ月」型の時期依存表現を持つ記事：

| ファイル | 状態 |
|---|---|
| `boki1-11month-strategy.html` | ❌ 「今日（7月）」「残り時間 約4ヶ月」＋図解2枚 |
| `boki1-next-exam.html` | 🟡 「2026年11月15日」と**絶対日付**で書いており、時期依存表現は含むが日付が明示されている |
| `boki2.html` | 🟡 要確認（今回の監査範囲外） |

**絶対日付で書いている `boki1-next-exam.html` は、時間が経っても壊れない。** 対照的に「今日（7月）」は書いた瞬間から劣化が始まる。

---

## CEOジョブズの所見

指摘1で私が重く見ているのは、3件の破綻そのものより、**第6と第7が同じ欠陥だったこと**である。

8月4日から今日まで、私はパターンを7つ数えた。数えるたびに検査条件を継ぎ足し、そのたびに「これで網羅した」と思った。**しかし今日わかったのは、6と7は同じものだったということである。つまり私は、同じ穴を2回別々の名前で発見していた。**

08-11に「最終適用色を両テーマで解決してコントラストを計算する以外にない」と結論を書いた。今回それを実装して回したら、パターンの区別を一切せずに3件出た。**結論は3か月前から正しかった。正しい結論を書いてから、それを実装せずにパターンを数え続けていたのは私である。**

指摘2は、性質が違う。これは誰のミスでもない。**「今日（7月）」と書いた時点では正しかった。** 問題は、その文が正しくいられる期間が1か月しかなかったことに、書いた側も更新した側も気づいていなかったことである。

そして5日前に更新の手が入っている。**日付フィールドは3箇所きちんと動いた。本文の「今日」は動かなかった。** ルールが効いている範囲と、効いていない範囲がはっきり分かれている。

---

## 承認いただきたいこと

### 対応1｜`.merit-card` / `.grad-card` / `.faq-item` のダーク規則を補う

**⚠️ 直し方が3件で異なる。一括では直らない。**

| 対象 | 直し方の候補 | 判断が要る点 |
|---|---|---|
| `univ-boki-units.html` `.merit-card p` | (A) widgets 1486 のセレクタに `p` を追加／(B) 記事側の `#374151` を `var(--muted)` に変更 | **(A)は194本の `.app-cta h3` と同じ「widgets 側で列挙を足す」対処で、また別の子要素で再発する。(B)は記事1本で完結するが、`.merit-card` を使う他の記事に効かない。** 反例の `boki-zero-02.html` は `.desc` を使って無傷なので、**どちらの方針を標準にするかを先に決める必要がある** |
| `boki-zero-05.html` `.grad-card` | widgets に `html[data-theme="dark"] .grad-card{background:...}` を1行追加 | 該当1本のみ。**ただし `.grad-card h3{color:var(--green2)}` はライトで 2.49:1 で、背景を暗くしても h3 の色は別途要検討** |
| `boki-zero-03.html` / `boki3-shohi.html` `.faq-item` | widgets に `html[data-theme="dark"] .faq-item{background:...}` を追加 | **widgets には `.faq-item` のダーク規則が 2243行と 2378行の2箇所にある。どちらに足すか、あるいは重複を先に整理するかを決める必要がある** |

### 対応2｜検査スクリプトを常設化し、**ライトとダークを対等に回す**

今回のスクリプトは以下を実装済みである。これを `company/reports/automation/` 配下に置いて毎回同じものを回す。

1. `linear-gradient()` / `radial-gradient()` の色解決
2. `!important` を含む詳細度の正しい優先順位
3. `var()` のテーマ別変数表引き（フォールバックは定義が無いときのみ）
4. ライト・ダークの両テーマで独立に計算
5. テキストを含まない要素（`<img>` の背景等）の除外
6. **【今回追加】テキストを持つ要素が自分自身の `background` を持つ場合、親ではなくそれに合成する**

**⚠️ 6を入れないと、全記事のCTAボタンが毎週偽陽性で出る。**

### 対応3｜`.app-cta h3`（194本・08-14稟議）の可否を先に決める

**指摘1-4は新しい発見ではない。4日前の稟議がそのまま残っているだけである。** ただし194本＝実質サイト全記事で、潰れているのはアプリ導線の見出しである。**対応1と同じ性質の修正なので、まとめて判断できる。**

### 対応4｜ブランド緑ボタン（2.05:1）を検討対象にするかを決める

**⚠️ 私は改善案を書かない。** サイトの中核配色であり、実際の見え方を確認していない。**取締役に事実だけ報告し、検討対象にするかどうかの判断を仰ぐ。**

### 対応5｜`boki1-11month-strategy.html` の時期表現を是正する

**⚠️ これは単純置換で直らない。**

- 「7月」→「8月」に置換すると、「約4ヶ月」→「約3ヶ月」、「合計約11ヶ月」→「約10ヶ月」、「7月〜9月 基礎インプット」の月別配分がすべて連動して変わる
- **「11ヶ月」は h1・title・h2・ファイル名（`boki1-11month-strategy.html`）に入っている。** 数字を動かすとファイル名と食い違う
- **図解2枚（`boki1-11month-strategy.svg` / `-mobile.svg`）にも「7月スタート」「7月〜9月」「約4ヶ月」が焼き込まれている。本文だけ直すと、08-04 `boki3-genka.html`（図解と本文の桁が1000倍違った）と同じ「図と本文の食い違い」を新しく作ることになる**

選択肢：

| 案 | 内容 | 影響 |
|---|---|---|
| **案A** | 相対表現をやめ、**絶対日付**で書き直す（「2026年11月15日の試験まで約○か月」ではなく「試験日は2026年11月15日」と書き、残り期間は読者に委ねる） | `boki1-next-exam.html` と同じ方式。**時間が経っても壊れない。** ただし記事の売りである「今から間に合う？」の切迫感は弱まる |
| **案B** | 毎年6月〜7月に「7月」へ戻す運用にする | **運用が止まった瞬間に今回と同じ状態になる。** 現に止まっていた |
| **案C** | 本文＋図解2枚を8月起点に作り直す | 今年だけ正しくなる。来年また同じ |

**私は案Aを推す。** ただし、この記事は「今年の11月に間に合うか」という検索意図に応える設計であり、**案Aにすると記事の性格が変わる。** そこは取締役の判断を仰ぐ。

### 対応6｜定型チェック項目に「時期依存表現」を新設する

**項目28（新設案）：記事本文と図解に、絶対日付でない時期表現（「今日（○月）」「残り約○ヶ月」「今から」「来年」）が無いかを確認する。** あれば、その表現が正しくいられる期間を見積もり、絶対日付に置き換えられないかを検討する。

**日付更新ルール（3箇所セット）は日付フィールドしか見ておらず、本文の「今」を見ていない。** 今回の記事は3箇所すべて正しく更新されたうえで、本文が1か月ずれていた。

---

**修正の実行は承認後に行う。本稟議の作成時点で記事ファイル・`article-widgets.js`・画像は一切編集していない。**
