# Study Quest — エージェント向け実務ルール

> **このファイルは要約である。実務ルールの正本は `CLAUDE.md`。**
> 作業に着手する前に必ず `CLAUDE.md` を読むこと。本ファイルは、**実際に事故が起きた項目**だけを抜き出して先頭に置いたものである。

---

## 0. 着手前チェック（毎回・例外なし）

```bash
# ① 今日の日付を確定させる（後述の理由により必須）
git log -1 --date=iso --format='%ad'

# ② 他のエージェントが作業中でないか
git status --short | wc -l
ls .git/index.lock 2>/dev/null && echo "他プロセスが作業中の可能性"
```

**このリポジトリは Codex・Claude・GitHub Actions・定期タスクが同時に触る。** 自分の変更と他者の変更を同じコミットに混ぜないこと。混ざると「なぜこの変更をしたか」が永久に追えなくなる。

---

## 1. 組織と意思決定

| 役職 | 誰 | 権限 |
|---|---|---|
| オーナー・取締役 | 人間（大谷） | 最終意思決定権 |
| CEO | ジョブズ（AI） | 実務の意思決定、戦略立案 |

### 提案の評価順（この順で判断する）

1. **収益に直結するか**（アフィリエイト導線・受注に繋がるか）
2. **見つけてもらえる状態に近づくか**（インデックス・検索表示回数）
3. **ユーザーの人生を本当に変えるか**（1と2が同点ならこれで決める）

### 稟議が必要なもの

- 記事コンテンツの変更（新規執筆・リライト・セクション追加・図解差し替え）
- `app.html` の改善（規模を問わず）
- デザイン変更・優先順位の大幅変更（規模の大きいもののみ）

**稟議書は `company/decisions/YYYYMMDD-*.md` に記録する。** 取締役は、稟議・根拠・実行範囲が記録された通常案件について、CEOジョブズ／Codexが実行まで進める包括委任をしている（2026-08-30決定）。そのため、個別に「取締役の承認待ち」として停止させない。

### 包括実行委任と自動実行キュー（2026-08-30 取締役決定）

- 稟議と必要なファクトチェックが揃った通常案件は、CEOジョブズ／Codexが実行・PR作成・検証・自動マージまで進めてよい。旧稟議の「取締役の承認待ち」は、それ自体では実行停止の理由にしない。
- **保留**は、稟議に判断質問が残る、取締役が明示して止めた、または後述の対象外に当たる場合だけに使う。**却下**は取締役が明示した場合だけに使う。
- 別の実行専用タスクが無人で扱えるのは、稟議・根拠・実行範囲が記録済みで、低リスクかつ機械検証可能な案件だけである。隔離clone、1案件1PR、`git diff --check`、対象に応じた構文・リンク検証、マージ競合なしを必須とする。
- 無人実行の対象外は、外部サービス設定、広告・AdSense・SNS投稿、契約・支出、ドメイン、規約・同意、個人情報、会社名・ミッション・大規模ピボット・予算である。
- `app.html`、新規記事・大幅リライト、LP／主要導線のUI・デザインは、稟議・必要な目視確認を省略しない。定期実行タスクによる無人UI変更は引き続き禁止する。

### 検証済みPRの自動マージ（2026-08-27 取締役決定）

包括実行委任の対象である稟議（または事実誤りの即時是正）に収まり、検証済みで無関係な変更を含まない独立PRは、Codexが`main`へ自動マージしてよい。取締役はGitHubでのマージ操作を代行しない。

マージ前に、最新`main`との差分、競合の有無、`git diff --check`、変更に応じた検証を確認する。競合・検証失敗・差分不明・広告／AdSense／契約／外部設定を含むPRは自動マージしない。マージ後はPR番号、マージコミット、検証結果を報告する。**稟議・根拠・実行範囲の記録を省略できる規則ではない。** 定期タスクの無人マージは、上記「包括実行委任と自動実行キュー」の条件を満たす場合に限る。

### 例外：事実誤りの是正は稟議を通さず即実行する（2026-08-05 取締役決定）

| 即実行してよい | 稟議が必要 |
|---|---|
| 貸借の逆転、仕訳の誤り | 記事の構成・章立ての変更 |
| 合格率・配点・試験日程・受験料の誤り | 新しいセクションの追加 |
| 計算結果の矛盾、図解の桁ズレ | 説明の切り口・たとえ話の変更 |
| 公式サイトと食い違う金額 | デザイン・UIの変更 |
| titleが約束した内容が本文に無い | 新規記事の企画 |

**ただし直したら必ず `company/reports/` に記録する。** 稟議を省くのは「事前の承認」であって「事後の記録」ではない。**判断に迷ったら稟議に回す。**

---

## 2. 数値・事実を書くときのルール

### 2-1. 公式で裏を取っていない数値は書かない

範囲（◯〜◯万円）で逃げない。書くときは**出典・取得日**をセットにする。確認できない場合は**掲載しない。** 空欄にして理由を書くほうが、間違った数字より読者の役に立つ。

**根拠となった事故（2026-07-31）：** 5資格の予備校費用を確認したら4資格で裏付けが取れていなかった。CPA会計学院を「40〜60万円・コスパ重視」と書いていたが、公式価格は**800,000円で4校中もっとも高かった**。「相場観で表を作る」という制作手順が原因だった。

### 2-2. 出典リンクを本文に置く（2026-08-13 取締役指示）

数値を引用したら、**読者から見える形で一次情報へのリンクを本文に置く。**

```html
<span style="font-size:12px;color:var(--sq-soft,#718096);">出典：<a href="https://..."
  target="_blank" rel="noopener">発行元「資料名」</a>（2026年8月13日確認）</span>
```

- リンク先は**試験実施団体・官公庁などの一次情報**とする。予備校・まとめサイトは出典にしない
- **自分が実際に開いて読んだページだけを出典にする。** 検索結果の要約だけで出典リンクを書かない
- E-E-A-T の観点でも、AdSenseの「有用性の低いコンテンツ」対策としても効く

### 2-3. 分母・前提を明記する

**根拠となった事故（2026-08-13）：** 司法書士の「合格率約4%」を稟議が「事実誤り」と指摘したが、法務省のデータを確認すると分母の違いだった。

```
出願者 17,365人 ／ 受験者 14,418人 ／ 合格者 751人
  受験者ベース  5.21%
  出願者ベース  4.33%   ← 記事の「約4%」はこちら。誤りではなかった
```

**稟議どおりに置換していたら、正しい数字を別の数字に書き換えただけになっていた。** 合格率・平均・割合を書くときは分母を明示する。

### 2-4. 成果保証表現・裏付けのない最上級表現を書かない

```
✗ 必ず合格できます／絶対に目指せます／確実に合格をもぎ取れる
✗ 最も不合格者を出す罠／最凶／最も多くの受験生が
```

合格率15〜36%の試験について成果を保証するのは**誤解を招く表現**であり、AdSenseポリシーに抵触しうる。また商工会議所は設問別の正答率を公表していないため、「最も」の裏付けは外部でも取れない。

**次は誤検出なので直さないこと：**「誰でも受験できます」（受験資格がない事実）／「絶対に再現できません」（否定の強調）／比較表内の「確実に合格を目指す人」（ペルソナ記述）。

---

## 3. 日付を書くときのルール

### 3-1. 日付を書く前に `git log` で今日を確認する

```bash
git log -1 --date=iso --format='%ad'
```

| 情報源 | 信頼できるか |
|---|---|
| セッション開始時に渡される日付 | **不可。** 会話が日をまたいでも更新されない |
| `date` コマンド | **不可。** 環境が古いまま起動していることがある |
| `git log --date=iso` | **可。** コミットの実時刻であり、ずれない |

**根拠となった事故（2026-08-12）：** CEOが丸一日「今日は8月5日」だと思い込んで作業した。上記2つが**両方とも同じ誤り**を返していたため突き合わせでは気づけず、21記事の更新日を1週間前で刻んだ。取締役の指摘まで発覚しなかった。

### 3-2. 日付は3箇所セットで更新する

1. JSON-LD の `"dateModified"`（`YYYY-MM-DD`）
2. 本文に表示している「更新日：YYYY年M月D日」
3. `sitemap.xml` の該当URLの `<lastmod>`

**1箇所でも取りこぼすと、構造化データ・画面表示・サイトマップが食い違い、検索エンジンに矛盾したシグナルを送る。** `datePublished > dateModified` の逆転がないかも確認する。

**実質的な本文変更**（事実是正、一次情報リンクの追加・差し替え、本文構成・内容の変更）は、分量にかかわらず3面を同日に更新する。キャッシュバスター、CSS・配色、サムネイル／OG画像、著者表記・定型フッター・CTAだけの変更では、3面を動かさない。

---

## 4. title と本文の整合

`<title>` を変更・新設したときは、次の4点を必ず突き合わせる。

1. `<title>` / `<h1>` / `og:title` / `<meta name="description">` が**同じ記事を指しているか**
2. titleに入れたキーワードが**本文（headの外）に実在するか**
3. titleが「〜との違い」「例題◯問つき」等を名乗るなら、**対応するh2・実体が本文にあるか**
4. 同じキーワードを別ページのtitleが既に使っていないか

**titleは本文の要約であって、集客のためのコピーではない。**

---

## 5. CSS・デザインの規約

### 5-1. `--sq-*` 変数にフォールバック付きで連携させる

```css
/* ✅ 正 */
color: var(--sq-text, #1A202C);
background: var(--sq-surface-soft, #F7FAFC);

/* ✗ 誤：固定色。テーマ切替に追従しない */
background: rgba(255,255,255,.82);

/* ✗ 誤：未定義の変数。背景が一切適用されない */
background: var(--card);
```

**根拠となった事故（2026-08-13）：** `.article-hero figcaption` が16ファイルで上の2パターンを使い、ダークモードのコントラスト比が **1.67:1** になっていた（WCAG AA の本文基準は 4.5:1）。修正後は 11.80:1。

### 5-2. カンマ区切りセレクタは各セレクタが独立評価される

```css
/* ✗ 誤：.b は全テーマに適用される */
html[data-theme="dark"] .a, .b { color:#C4B5FD !important; }

/* ✅ 正 */
html[data-theme="dark"] .a, html[data-theme="dark"] .b { color:#C4B5FD !important; }
```

**根拠となった事故（2026-08-12）：** 6箇所でテーマ限定が外れ、**ライトモードの金額欄がコントラスト比 1.43:1**（事実上見えない）になっていた。W3C Selectors Level 3 の仕様どおりの挙動でありブラウザ差はない。

### 5-3. 共通クラスを使うときはローカルCSSの定義を確認する

`article-widgets.js` が `.tip-box` / `.warn-box` 等に当てるのは **色だけ**（`background` と `border-color`）。`padding`・`border-width`・`border-radius`・`margin` は当たらない。**定義が無いまま使うと、色だけ付いて余白も枠線も無い崩れた見た目になる。**

逆に、記事固有の `<style>` でクラス名を定義するときは、widgets側の標準クラス名（`.tip-box` `.warn-box` `.formula` `.flow-item` `.journal` `.num-box` `.rank-card` `.lead` 等）と**衝突しないか確認する。** 衝突すると `!important` 付きの共通スタイルに上書きされる。

### 5-4. 新規SVG図解は暗い背景でも成立させる

白い背景矩形（`<rect fill="#FFFFFF">` 等）を敷かず、**透過背景＋中間色の線・文字**で作る。`<img>` 経由のSVGにはページのCSSが届かず、SVG内部の `@media (prefers-color-scheme:dark)` は**OSの設定に従うためサイトのSystem/Light/Darkトグルと連動しない。**

---

## 6. デザイン保護ルール（2026-08-12承認）

Study QuestのLP・記事導線は、単なる装飾ではなくプロダクトの認知資産である。

- 基調は **Dark Mode Digital / Study Quest Field System**。黒〜深緑の背景、細いグリッド、蛍光イエローグリーンを主アクセントとする
- 紫〜ピンクのグラデーション、安易な中央揃え、均等な3カラムカードの連続、意味のない絵文字への置き換えをしない
- 主要導線はPCで「移動・わずかな回転・硬い影」、モバイルで「タップ開始時の即時フィードバック」を維持する。本文段落を飾りのために動かさない。`prefers-reduced-motion` を必ず尊重する
- **LPの大幅な配色・レイアウト・タイポグラフィ・インタラクションの変更は、取締役の明示承認とPC/モバイルの目視確認なしに行わない**
- **定期タスクによる無人UI書き換えは引き続き禁止する。** ただし、UIを含まず、稟議・根拠・実行範囲が記録済みで低リスクかつ機械検証可能な案件は、上記「包括実行委任と自動実行キュー」に従い、実行専用タスクが隔離cloneでPR作成・自動マージまで行ってよい。
- 実装前に `company/strategy/20260812-design-guardrails.md` を読む

---

## 7. 記事を公開・更新するときのチェックリスト

新規記事を公開する場合：

1. `index.html` の該当カテゴリに記事カードを追加
2. `index.html` の「更新情報」（`.updates-list`）の**先頭**に追記（新記事＝`feature` / 改善＝`improve` / 修正＝`fix`）
3. 記事の更新情報は `index.html` にのみ記載する（`app.html` の更新情報には載せない）
4. 資格ガイド本体を新設した場合は `shikaku-list.html` の「難易度から探す」にも追加（難易度は推測せず、**その記事が本文で示している勉強時間**に従う）
5. シリーズ記事は `article-widgets.js` の `BOKI*_FILES` 等の配列**と** `ARTICLES` の**両方**に登録する
6. **本文が3,000字に満たない記事にはGoogle広告枠（`<ins class="adsbygoogle">`）を入れない。** ライブラリ読込だけは広告枠ではない。3,000字はGoogleの基準ではなく自社基準である。例外は `scripts/audits/run-adsense-policy-gate.mjs` の `ALLOWED_SHORT_MONETIZED` に理由付きで記録し、公開前に `node scripts/audits/run-adsense-policy-gate.mjs --strict --no-write` を通す。
7. **Google広告枠を新設・維持する記事は、本文ごとに独自性レビューを残す。** その記事でしか得られない判断・例題・検証・体験のどれか、変動事実の一次情報、近接するシリーズ記事との重複確認を稟議またはPRに記録する。コピー、類義語置換、未レビューの自動生成を広告枠に載せない。根拠が不足する記事は広告枠を増やさず、先に本文を見直す。
8. **AdSenseの所有権確認・再申請・広告設定は取締役がAdSense画面で行う。** エージェントは事前監査とPR作成までを担当し、外部サービス側の操作完了を推測しない。

**`ARTICLES` を引けなかったファイルは、エラーも出さず黙って捨てられる**（`const a = ARTICLES[f]; if(!a) return;`）。配列にだけ追記して `ARTICLES` を忘れると、その記事はサイドバー・前後ナビ・新着・人気から消える。記事自体は表示されるので気づかない。

---

## 8. 稟議書を書くときのルール（2026-08-12追加）

- **指摘が複数ある場合、冒頭に「本稟議の指摘は N 件」と明記する。** 実行側が2件目を読み飛ばす事故が起きた
- **その件数が「今回監査した15本の中での件数」なのか「全記事スキャンでの実数」なのかを明記する**
- **修正方法を断定せず、判断が要る点を明示する**

**過去に3回、同じ取りこぼしが起きている。**

```
2026-07-29  ペルソナ違反   1件 → 5件 → 38件 → 52ファイル69箇所
2026-07-31  予備校費用     「金額の記載0箇所」→ 実際は万円表記で多数
2026-08-05  成果保証表現   4箇所 → 19箇所
```

**稟議の件数をそのまま作業量として受け取らない。着手前に必ず全記事スキャンで測り直す。**

---

## 9. コーディング規約

- バニラJS・HTML・CSSのシンプル構成を維持する
- 外部ライブラリの追加は「本当に必要か」を最初に問う
- データは localStorage に保存（サーバーレス構成）
- モバイルファーストのUI設計

---

## 10. 詳細は CLAUDE.md を参照

本ファイルに載せていない内容（会社の収益目標と達成プロセス、CEOジョブズの人格設定、ディレクトリ構成、各ルールが生まれた詳細な経緯）は `CLAUDE.md` にある。**矛盾がある場合は `CLAUDE.md` が優先される。**
